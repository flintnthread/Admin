# Admin API (admin-backend on port 8082, binds 0.0.0.0)

# Web on this PC (browser desktop / localhost)
EXPO_PUBLIC_ADMIN_API_WEB_BASE_URL=http://localhost:8082

# Native mobile (Expo Go auto-detects PC IP from Metro — optional override):
# EXPO_PUBLIC_ADMIN_API_BASE_URL=http://192.168.1.5:8082
#
# Only set EXPO_PUBLIC_ADMIN_API_BASE_URL if auto-detect fails on a physical phone.
# Find your PC IP: ipconfig (Windows) → IPv4 Address

# Login: DB admin e.g. admin@gmail.com / 12345
# Or bootstrap: admin@flintnthread.in / Admin@12345
